// src/config.js
// Use a local API base URL during development, otherwise use production
const API_BASE_URL = 'https://api.ratedeed.com'; // Always use production API base URL

export { API_BASE_URL };
